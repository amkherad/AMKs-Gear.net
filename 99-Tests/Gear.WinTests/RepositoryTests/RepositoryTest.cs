namespace Gear.WinTests.RepositoryTests
{
    public class RepositoryTest
    {
        public static void Test()
        {
            //var context = TestDataContext.Generate(100).ToArray();
            //using (var repo = new FooRepository())
            //{
            //    Logger.Write(repo.Count());

            //    repo.SaveOrUpdate(context.Select(x => new Foo
            //    {
            //        Name = x.FullName,
            //        Author = x.FullName + x.Email,
            //        Description = x.Email,
            //        Size = x.Age,
            //        Price = x.Age
            //    }));
            //    repo.SaveChanges();

            //    Logger.Write(repo.Count());
            //}
        }
    }
}
